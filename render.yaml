services:
  - type: web
    name: coderefine-backend
    env: docker
    branch: main
    dockerfilePath: Dockerfile
    plan: free
    healthCheckPath: "/"
    envVars:
      # Add your secrets here via the Render dashboard or replace the placeholders below.
      - key: OPENAI_API_KEY
        value: ""
      - key: GOOGLE_API_KEY
        value: ""
